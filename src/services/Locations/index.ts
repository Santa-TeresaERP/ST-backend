import createLocation from './serviceCreateLocation'
import deleteLocation from './serviceDeleteLocation'
import getAllLocations from './serviceGetAllLocations'
import getLocationByID from './serviceGetLocationByID'
import updateLocation from './serviceUpdateLocation'

const useLocation = {
  createLocation,
  deleteLocation,
  getAllLocations,
  getLocationByID,
  updateLocation,
}

export default useLocation
